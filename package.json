{
  "name": "musreact",
  "version": "0.2.0",
  "description": "Mustache to React Component compiler",
  "scripts": { "test": "node test/all.js", "compile": "jison grammar.jison -o musreact.js" },
  "dependencies" : {
    "jison": ">=0.4" 
  },
  "devDependencies": {
    "test": ">=0.6.0",
    "react": ">=0.12.1"
  }
}
